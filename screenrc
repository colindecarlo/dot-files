# load my bash_profile
#exec $HOME/.bash_profile

# don't display the startup message
startup_message off

# change scrollback to 10000 lines
defscrollback 10000

# status line
hardstatus on
hardstatus alwayslastline
hardstatus string "%{.bW}%-w[%f%n %t]%+w %=%{..Y} %Y-%m-%d %0c"
